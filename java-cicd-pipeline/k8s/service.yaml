apiVersion: v1
kind: Service
metadata:
  name: devops-demo-app-svc
  namespace: devops-demo
spec:
  type: LoadBalancer
  selector:
    app: devops-demo-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
